# Files In `example`

This directory contains static example data used by the sample application.

- `forms/`: Sample form payloads served by the local static server. This mimics a future Engagement delivery endpoint without requiring a backend for the lightweight prototype.

